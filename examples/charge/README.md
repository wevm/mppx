# Charge

A barebones example demonstrating a payment-gated image generation API.

```bash
npx gitpick wevm/mppx/examples/charge
pnpm i
pnpm dev
```

## Test with mppx CLI

With the server running, use the `mppx` CLI to make a paid request:

```bash
mppx localhost:5173/api/photo
```
