---
"mppx": minor
---

Added `autoSwap` flag to Tempo client charge method. When enabled, the client automatically swaps from fallback currencies (e.g. pathUSD, USDC) via the DEX if the payer lacks the target token.
