---
'mppx': patch
---

Added Tempo TIP-1034 precompile channel helpers for computing channel IDs, expiring nonce hashes, vouchers, ABI calldata, open validation, descriptor persistence, precompile session credential payload parsing, client credential builders, and an opt-in precompile client session method.
