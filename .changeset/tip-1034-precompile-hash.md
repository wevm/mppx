---
'mppx': patch
---

Added Tempo TIP-1034 precompile channel helpers for computing channel IDs, expiring nonce hashes, vouchers, ABI calldata, open/top-up validation, descriptor persistence, precompile session credential payload parsing, client credential builders, opt-in client sessions, and opt-in server session verification.
